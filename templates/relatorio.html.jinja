<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="utf-8">
    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <style type="text/css">
        .container {
          max-width: 1000px;
          padding-top: 10px;
        }
        p {
          font-size: medium;
          text-align: left;
          display: flex;
          flex-direction: column
        }
        h1 {
          text-align: center;
        }
    </style>
    <title>Relatório {{ pronac }}</title>
  </head>
  <body>
    <div class="container">
      <div class="row text-center justify-content-center">
        <h1><b>Relatório de similaridades do pronac {{ pronac }} realizado em {{ date }}</b></h1>
        <hr style="border: 1px solid black; width: 650px;"/>
      </div>

      <h3>Foram comparados {{ analisados|length }} pronacs nesse relatório</h3>
      <ul>
      {% for item in analisados %}
        <li>{{ item.pronac }}: {{ item.arquivos }} arquivos encontrados e {{ item.similaridades }} similaridades encontradas.</li>
      {% endfor %}
      </ul>

      <br />

      <div class="container">
      {% for item in similares %}
        <h3>Similaridades encontradas em {{ item.pronac }}:</h3>
        {% for si in item.similaridades %}
          <p>Similaridade: <b>{{ si.score }}%</b></p>
          <div class="row">
            <div class="col">
              <img src="{{ si.image1 }}" height="300px" width="300px" />
            </div>
            <div class="col">
              <img src="{{ si.image2 }}" height="300px" width="300px" />
            </div>
          </div>
          <hr style="border: 1px dashed black;"/>
        {% else %}
            <p><b>Não foram encontradas similaridades entre as imagens analisadas.</b></p>
        {% endfor %}
        <hr style="border: 2px solid black;"/>
      {% endfor %}
    </div>

    </div>
  </body>
</html>